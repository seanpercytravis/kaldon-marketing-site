---
interface Props {
  href: string;
  class?: string;
  [key: string]: any;
}
const { href, class: className = '', ...rest } = Astro.props;
---

<a href={href} class={`link-arrow ${className}`.trim()} {...rest}>
  <slot />
  <span class="arrow" aria-hidden="true">→</span>
</a>
